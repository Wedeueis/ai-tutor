{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "OKF concept — base schema",
  "description": "The always-valid common fields (WIKI_SPEC.md §4.1). Used as the fallback for any `type` without its own registered schema; add `<Type Name>.schema.json` alongside this file to add stricter, type-specific rules.",
  "type": "object",
  "required": ["type"],
  "properties": {
    "type": { "type": "string", "minLength": 1 },
    "title": { "type": "string" },
    "description": { "type": "string" },
    "resource": { "type": "string" },
    "tags": { "type": "array", "items": { "type": "string" } },
    "status": { "type": "string", "enum": ["draft", "stable", "deprecated"] },
    "stale_after": { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" }
  }
}
