# References

External material, run instructions, and code that concepts cite for
provenance or attestation (`sources[].resource`, `executor.resource`,
`attester.resource` — OKF §6.3, §5.1, §10).

*(empty — populate as concepts need to cite external material)*
